# ── OS / System ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Obsidian — per-device settings, never tracked (web UI replaces shared config) ───
.obsidian/

# ── AI Agents ─────────────────────────────────────────────────────────────────
.claude/settings.local.json
.claude/onebrain.local.md

# ── Git ───────────────────────────────────────────────────────────────────────
.worktree/
.worktrees/

# ── Internal tooling ──────────────────────────────────────────────────────────
.superpowers/
docs/superpowers/
# Local diagram preview harness (one-off audit tool, not for repo)
preview.html

# ── Node / Bun ────────────────────────────────────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
*.bun-build
