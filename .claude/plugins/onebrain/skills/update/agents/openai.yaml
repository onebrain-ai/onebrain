interface:
  display_name: "OneBrain Update"
  short_description: "Update OneBrain managed files"
  default_prompt: "Use $onebrain:update to update OneBrain."
policy:
  allow_implicit_invocation: false
